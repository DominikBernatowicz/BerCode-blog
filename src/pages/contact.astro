---
import { CutCornerButton } from "../components/CutCornerButton";
import { FormInput } from "../components/form/FormInput";
import { FormTextarea } from "../components/form/FormTextarea";
import InteriorContent from "../components/InteriorContent.astro";
import InteriorHero from "../components/InteriorHero.astro";
import DefaultLayout from "../layouts/DefaultLayout.astro";
---

<DefaultLayout>
    <InteriorHero>
        <div class="max-w-3xl mx-auto py-4 lg:py-16">
            <p
                class="uppercase font-extrabold text-center text-zinc-500 tracking-wider"
            >
                Skontaktuj się ze mną
            </p>

            <h1
                class="font-heading font-black text-3xl md:text-5xl lg:text-6xl text-center mt-4 max-w-lg mx-auto"
            >
                Pozostańmy w kontakcie
            </h1>

            <p
                class="text-center text-xl md:text-2xl mt-6 text-zinc-400 max-w-60 md:max-w-md mx-auto"
            >
                Masz pytania dotyczące moich projektów lub pomysł na współpracę?
                A może chcesz po prostu nawiązać kontakt?
            </p>

            <p
                class="text-center text-xl md:text-2xl mt-4 text-zinc-400 max-w-60 md:max-w-md mx-auto"
            >
                Napisz śmiało, a z przyjemnością odpowiem.
            </p>
        </div>
    </InteriorHero>
    <InteriorContent>
        <div class="py-16 max-w-xl mx-auto">
            <h2 class="font-heading font-black text-3xl">
                Mogę w czymś pomóc?
            </h2>
            <form class="flex flex-col mt-8" name="contact" method="POST" data-netlify="true">
                <label for="name">Imię</label>
                <FormInput id="name" name="name" type="text" className="mt-1" />

                <label for="email" class="mt-4">Email</label>
                <FormInput
                    id="email"
                    name="email"
                    type="email"
                    className="mt-1"
                />

                <label for="message" class="mt-4">Wiadomość</label>
                <FormTextarea id="message" name="message" className="mt-1" />

                <CutCornerButton type="submit" className="mt-4">
                    Wyślij wiadomość
                </CutCornerButton>
            </form>
        </div>
    </InteriorContent>
</DefaultLayout>
